# Base URL of the Python backend. Leave unset/empty to run against local mock data.
# Do not place secrets in Vite client variables — they are exposed to the browser.
VITE_API_BASE_URL=http://localhost:3000/api

# Request timeout in milliseconds before a search is treated as a timeout error.
VITE_API_TIMEOUT_MS=10000

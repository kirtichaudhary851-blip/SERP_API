# Custom SERP API Frontend

Phase 0 frontend foundation built with React, Vite, React Router, Tailwind CSS, and ESLint.

## Commands

```bash
npm install
npm run dev
```

Additional checks: `npm run lint` and `npm run build`.
